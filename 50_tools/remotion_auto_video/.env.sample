# remotion_auto_video sample overrides
#
# すべての API キーは `/Users/dd/youtube_master/.env` に集約されています。
# このファイルはテンプレートとして残し、どうしても別キーを使いたい場合のみ複製して `.env` を作成してください。

# 例: Pixabay を別アカウントで利用する場合
# PIXABAY_API_KEY=your-own-key

# 例: VoiceVox API を別 URL で使う場合
# VOICEVOX_API_KEY=xxxx
# VOICEVOX_API_BASE_URL=https://api.su-shiki.com/v2/voicevox
# VOICEVOX_DAILY_POINTS=10000000

# 例: ローカル VoiceVox の URL を指定する場合
# VOICEVOX_SERVER_URL=http://127.0.0.1:50021
