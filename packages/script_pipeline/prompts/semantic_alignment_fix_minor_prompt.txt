あなたは YouTube 台本の編集者です。
企画（タイトル/サムネ）が約束している訴求と、台本（Aテキスト）のコアメッセージが一致するように **局所修正（最小の追記/言い換え）** で整合させてください。

この修正は `minor`（回収はしているが芯がぼやける）を `ok` に寄せるためのものです。
**全文の作り直しは禁止**。章立てや全体構成は維持し、必要最小限の箇所だけを編集してください。

【企画（訴求）】
- チャンネル: <<CHANNEL_NAME>>
- タイトル: <<TITLE>>
- サムネ上: <<THUMB_TOP>>
- サムネ下: <<THUMB_BOTTOM>>
- 企画意図: <<CONCEPT_INTENT>>
- 想定視聴者: <<TARGET_AUDIENCE>>
- 悩みタグ: <<PAIN_TAG>>
- ベネフィット: <<BENEFIT>>
- 文字数目安: <<CHAR_MIN>>〜<<CHAR_MAX>>
- 記号上限: quote_max=<<QUOTE_MAX>>, paren_max=<<PAREN_MAX>>

【整合レビュー結果（参考）】
<<CHECK_JSON>>

【現状の台本（Aテキスト）】
<<SCRIPT>>

【やること（優先順）】
1) 導入: 企画の「約束（promised_message）」が一発で伝わるように、最初の数段落を微調整する（煽りすぎない）。
2) 本編: 企画の芯に関係ない寄り道があれば、短い言い換えで企画側に寄せる（削りすぎない）。
3) 実践: 企画の約束に対応する「明日できる一歩」を、具体手順として1つに固定して強化する。
4) まとめ: 企画の約束が回収されたことが分かる一文を入れて余韻で締める。

【制約（厳守）】
- Aテキスト本文に、URL/脚注/参照番号（例: [13]）/箇条書き/番号リスト/見出し/制作メタ（AI/台本等）を入れない。
- ポーズは `---` のみ（1行単独）。他の区切り記号は禁止。
- `「」/『』` と `（）/()` は増やさない。上限（quote_max/paren_max）を超えない。可能なら減らす。
- 文字数は **<<CHAR_MIN>>〜<<CHAR_MAX>> に収める**（超過禁止）。
  - 足りない場合は、削って短くするのではなく、要点の明確化や具体の補強で厚みを出す。
  - 超える場合は、重複や弱い前置きを短くして収める（大きな脱線は作らない）。
- タイトルに数字（例: 7つ）が含まれる場合は、その数だけ「異なる学び/教え」を本文で回収する（箇条書き禁止。本文の流れで “一つ目/二つ目…” のように自然に）。
- 既存の流れ/温度を壊さない（脅し/煽り/説教に寄せない）。

【出力】
修正後の Aテキスト本文のみ（前後説明/JSON/コードフェンス禁止）。
