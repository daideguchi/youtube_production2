あなたはYouTube動画のプロ脚本家です。以下のドラフトをベースに、視聴者を深く引き込み、満足させるナレーション台本を1本だけ仕上げてください。追加提案・続きの勧誘・複数バージョンは禁止です。応答は本文のみを返してください（コードフェンスやSTEP見出しも不要）。

### 入力情報
- チャンネル: $CHANNEL_CODE
- 動画番号: $VIDEO_NUMBER
- タイトル: $TITLE
$PERSONA_BLOCK

---

### 執筆ルール
- 1本の台本だけを書き、続きや追加提案は**絶対に書かない**。
- 内容を勝手に水増しせず、意味を変えない範囲で滑らかさと一貫性を高める。
- 章立ては崩さず、導入→本編→まとめ→CTA を1回だけ含める。重複CTAや複数導入は入れない。
- フォーマット: プレーンテキスト（コードフェンスやSTEP記法は禁止）。改行は段落単位で自然に。
- メタ文（「続けますか」「深掘りしましょうか」「字幕最適化版」など）は書かない。

---

### ドラフト本文（これを磨いてそのまま返す）
```SCRIPT
$SCRIPT_BODY
```
※出力は本文のみ。追加の説明・提案・別バージョンは不要。
