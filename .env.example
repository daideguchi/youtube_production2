# 必要な環境変数の例（実際のキーは設定してください）
# 注意: PYTHONPATHはfactory_commentaryパッケージが正しくインストールされている場合、設定不要です

# OpenRouter / Gemini / Azure
OPENAI_API_KEY=your_openrouter_or_openai_key
# Geminiキーはここ (.env) に必ず置く。別ファイル (.gemini_config 等) は使わない。
GEMINI_API_KEY=your_gemini_key
AZURE_OPENAI_API_KEY=your_azure_key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# ImageClient (画像生成) - optional overrides
# IMPORTANT (cost/drift guard):
# - 通常運用（YTM_ROUTING_LOCKDOWN=1）では、`.env` に IMAGE_CLIENT_FORCE_MODEL_KEY* を恒久セットしない。
#   - SoTで固定: channel presets / thumbnails templates
#   - その実行だけ変更: `IMAGE_CLIENT_FORCE_MODEL_KEY_<TASK>=... ./ops ...`（prefixで明示）
# - Task key examples: visual_image_gen / thumbnail_image_gen / image_generation
# IMAGE_CLIENT_PROFILE=default
# IMAGE_CLIENT_FORCE_MODEL_KEY_VISUAL_IMAGE_GEN=gemini_2_5_flash_image
# IMAGE_CLIENT_FORCE_MODEL_KEY_THUMBNAIL_IMAGE_GEN=openrouter_gemini_2_5_flash_image_preview
# IMAGE_CLIENT_CONFIG_PATH=configs/image_models.yaml
# IMAGE_CLIENT_TASK_OVERRIDES_PATH=configs/image_task_overrides.yaml
# LLM Router logging (optional)
# LLM_ROUTER_LOG_PATH=logs/llm_usage.jsonl
# LLM_ROUTER_LOG_DISABLE=0

# Fireworks
# - Image generation (ImageClient)
# Preferred: dedicated image key (new)
FIREWORKS_IMAGE=your_fireworks_image_key
# Backward-compat (legacy): FIREWORKS_API_KEY=...
FIREWORKS_API_KEY=your_fireworks_image_key
# Optional fallback key (legacy config may use this name)
# FIREWORKS_API_KEY_FALLBACK=your_fireworks_fallback_key
# FIREWORKS_ACCOUNT=fireworks
#
# - Script/text generation (LLMRouter provider=fireworks)
# Preferred: dedicated script key (new)
FIREWORKS_SCRIPT=your_fireworks_script_key
# Backward-compat (legacy): FIREWORKS_SCRIPT_API_KEY=...
# FIREWORKS_SCRIPT_API_KEY=your_fireworks_script_key

# Pipeline fallback（枯渇時に自動で使うモデル）
SCRIPT_PIPELINE_FALLBACK_MODEL=gpt-5-mini
SCRIPT_PIPELINE_FORCE_FALLBACK=1

# その他（必要に応じて）
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
# VOICEVOX related, etc.

# Google Drive (factory_commentary)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/google-credentials.json
DRIVE_FOLDER_ID=your_drive_folder_id

# YouTube Publisher (OAuth)
YT_OAUTH_CLIENT_PATH=/path/to/your/drive_oauth_client.json
YT_OAUTH_TOKEN_PATH=/path/to/your/youtube_publisher_token.json
YT_PUBLISH_SHEET_ID=your_spreadsheet_id
