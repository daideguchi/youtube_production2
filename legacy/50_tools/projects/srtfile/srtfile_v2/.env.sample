# srtfile_v2 sample overrides
#
# 実稼働の環境変数は `youtube_master/.env` に集約済みです。
# このファイルを `.env` にコピーし、どうしても別値にしたいキーだけを記入してください。

# 例: OpenRouter モデルを固定したい場合
# OPENROUTER_MODEL=qwen/qwen3-coder:free

# 例: 個別の Sheets/サービスアカウントを使う場合
# GOOGLE_SHEETS_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GOOGLE_SERVICE_ACCOUNT_JSON=/path/to/service-account.json

# ルート `.env` と矛盾するキーを増やすと、他ツールとの整合が崩れます。
# まずは `/Users/dd/youtube_master/.env` を更新し、それでも必要なときだけ本ファイルで上書きしてください。
