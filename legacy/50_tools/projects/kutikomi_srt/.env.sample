# kutikomi_srt sample overrides
#
# 本ツールは `youtube_master/.env` を唯一の実稼働ファイルとします。
# プロジェクト固有値を上書きしたい場合だけ、このファイルを `.env` にコピーして
# 必要なキーのみを記入してください（値は例です。全て任意）。

# 例: Gemini モデルを変更する場合
# GEMINI_MODEL=gemini-2.0-pro

# 例: プロジェクト固有の入出力設定
# PROJECT_NAME=kutikomi_srt
# OUTPUT_DIR=./output
# INPUT_DIR=./input

# 例: SRT 生成のレイアウト設定
# MAX_LINE_CHARS=24
# MAX_LINES=4
# SCENE_MAX_LINES=8

# ルート `.env` に含まれる API キー・シークレットはここに書かないでください。
# `source /Users/dd/youtube_master/.env` を実行してからツールを起動すれば十分です。
