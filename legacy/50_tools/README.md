# 50_tools

補助ツール・テンプレ専用の置き場です。運用ルールや TODO は `../ssot/README.md` を唯一の参照とし、ここにはツール本体以外を残しません。

- 実稼働の環境変数はルート `.env` に統一します。必要であれば `*.env.sample` をテンプレとして提供し、実値は `.env` へ記載してください。
- 出力物やログは `logs/` など正規ディレクトリに保存します。このフォルダには生成物を置かないでください。
- 過去の資料は削除済みです。参照が必要になった場合は git の履歴から取得してください。
