# legacy/scripts/route_audio

This folder keeps **legacy reference-only** scripts that predate the current SSOT-driven pipeline.

Do not use these for production runs. Current entrypoints live in `ssot/OPS_ENTRYPOINTS_INDEX.md`.

