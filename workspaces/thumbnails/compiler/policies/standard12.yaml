id: standard12
name: Standard 12 (bg × subject × fx)
version: 1

limits:
  max_variants: 12

selectors:
  backgrounds_glob: "backgrounds/*"
  subjects_glob: "subjects/*"
  belts_glob: "belts/*"
  fx_glob: "fx/*"

strategy:
  mode: cartesian
  deterministic: true
