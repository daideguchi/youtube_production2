# workspaces/notes — 人間用メモ/下書き（git管理）

目的:
- 個別作業のメモ/下書き/一時文章を **repo root に散らかさない**。
- スマホ（GitHub）からも参照しやすくする。

ルール:
- ここは **実行コード/パイプラインの入力として参照しない**（= SoTではない）。
- 追加する場合は分類して置く（例: `articles/`, `scripts/`）。
- フロー確定前のローカル試作（git非管理）は `workspaces/_scratch/` を使う。

