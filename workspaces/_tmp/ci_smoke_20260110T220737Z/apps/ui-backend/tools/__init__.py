"""
UI tools package.

Modules in this package provide operational tooling for the unified UI stack
(React + FastAPI) without touching the underlying workflow logic.

実行はスクリプト直叩きが正本:
  `python3 apps/ui-backend/tools/<tool>.py ...`
"""
