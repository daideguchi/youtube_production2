# Channels Directory
- 各チャンネルの設定・メタデータ置き場（例: `channels.json` / `channels_info.json` / CHxx-* フォルダ）。
- 台本生成や進捗 SoT は `data/CHxx/NNN/status.json` 側を正とし、ここは補足情報のみ。
- 新規ファイルを置く場合は用途を1行でコメントし、`ssot/history/HISTORY_codex-memory.md` に変更理由を記録する。***
