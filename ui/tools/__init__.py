"""
UI tools package.

Modules in this package provide operational tooling for the unified UI stack
(React + FastAPI) without touching the underlying workflow logic.  Each tool
is callable via ``python -m ui.tools.<module>``.
"""
